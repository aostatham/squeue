[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sequenced-queue"
version = "0.1.0"
description = "HTTP client for sequenced-queue"
requires-python = ">=3.10"
dependencies = ["requests>=2.31"]

[tool.pytest.ini_options]
testpaths = ["tests"]
